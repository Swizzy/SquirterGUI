FTCSPI.dll = x86
FTCSPI64.dll = x64

Use the correct one for the CPU you are running as!! (for best compatibility use x86)

You'll also need to have the FTDI drivers installed, they can be found here: http://www.ftdichip.com/Drivers/D2XX.htm you'll want the D2XX drivers